{
  "name": "go-websocket-template",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@asyncapi/generator-react-sdk": "^1.0.3"
  },
  "generator": {
    "renderer": "react",
    "parameters": {
      "moduleName": {
        "description": "name of the go module to be generated",
        "default": "go-async-api"
      }
    },
    "generator": ">=1.3.0 <2.0.0",
    "supportedProtocols": [
      "websocket"
    ]
  }
}
